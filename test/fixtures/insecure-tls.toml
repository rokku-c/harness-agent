[providers.claude]
api = "anthropic.messages"
model = "claude-test"
apiKey = "${TEST_ANTHROPIC_KEY}"

[insecureTls]
enabled = true

[composedAgents.claudeCode]
provider = "claude"

[composedAgents.codexOnly]
provider = "claude"
insecureTls = false
