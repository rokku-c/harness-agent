/**
 * Community adapters live here or in independent packages. They may depend on
 * @effect-agent/core contracts, but core never depends on community code.
 */
export const Community = {
  namespace: "@effect-agent/community"
} as const
