/**
 * @effect-agent/memory — L2 memory layer
 *
 * remember/recall/promote contract + default implementation over Store
 * (keyword scoring). The promotion hook is a pluggable learning gate
 * (nclawyp episode→lesson→skill).
 */
export * from "./memory.ts"
