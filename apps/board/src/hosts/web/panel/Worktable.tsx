/** Barrel: Worktable split by CONCEPT (see ./worktable/).
 *  table.tsx = container (view state + head/body); derive.ts = pure column/
 *  filter/sort derivation; toolbar.tsx = filter chips; row.tsx = one work-
 *  item row + its state-legal quick actions; table-types.ts = shared props. */
export { Worktable } from "./worktable/table.tsx"
