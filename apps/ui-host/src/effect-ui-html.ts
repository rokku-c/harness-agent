/**
 * ui-host UI as a raw HTML description (HTML is a supported description
 * language too — the render/generate pipeline is language-neutral).
 */

export const effectUiHtml = `<section data-ui="ui-host/html" style="font:15px system-ui;padding:10px;border:1px dashed #c9c5bd;border-radius:10px">
<h3>UI Canvas <small style="color:#8b877f">· raw html description</small></h3>
<p style="color:#555">A description can be authored as effect-ui view, json-render spec, or plain HTML.</p>
</section>`
