# formal/

The Lean model of the mechanisms the product rests on. Each module proves the
invariants one source file owes, and names that file at the top.

Lean core only — no mathlib. `bun run check:proofs` builds the model; the
guards list it alongside the code checks, so a proof that stops holding fails
like a broken test.

`Formal.lean` imports every module, and the check fails if one is missing:
`lake build` compiles that root's import closure and nothing else, so a module
absent from the list would be counted here while never being compiled.

| module | models | proves |
|---|---|---|
| `Formal/Adapt.lean` | `apps/effect-server/src/client/adapt/contract.ts`, `apps/effect-server/src/client/adapt/controls.ts` | the content and the attributes are duals: a node's content arrives on the `value` prop unless the node declared children, when `value` is the component's own, so dropping the key unconditionally loses the prop a compound component matches an item by and keeping it unconditionally hands a heading a stray attribute beside its own text — and neither throws. What a handler is handed is a fact about the component rather than a guess about the prop's name: exactly two components report the DOM event their own input raised, and the replaced rule compared the prop with `checked`, which is wrong for exactly those two, so view state receives an event where a string was expected. The handler is derived from the prop with one named exception, and that exception is the same fact as the argument's shape, so one flag decides both: the prop alone derives `onValueChange` for a text field, which the library never calls, so an input whose `value` nothing writes back is read-only to React and what was typed is dropped with nothing reported. A hand-written `{ prop, handler }` pair keeps the room a mismatch needs, which is why the pair was replaced by the derivation rather than kept beside it |
| `Formal/AppLayer.lean` | `apps/effect-server/src/boot/app-layer.ts` | suspending an app keeps its place, so teardown still runs in reverse load order; removing and appending it instead moves it to the front of teardown |
| `Formal/Arming.lean` | `agentd/src/launch-types.ts`, `apps/agentd/src/ops/launch-ops.ts`, `agentd-probe/src/launch-order.ts`, `agentd-probe/src/launch-cycle.ts`, `agentdeck/src/adapters/cli-preset.ts` | a launch is one of two things, and the difference is *who the machine is* when it runs: a turn runs as an identity — naming the machine, the dialect to spawn and the credential the door will verify — and a command runs as itself and has no identity at all. Carry the machine and the dialect beside the identity, as fields a caller may set, and every combination becomes a request somebody can make, so naming an identity constrains nothing and the config the center planned and the process that started agree only by luck; read them out of the fleet record under the request's own identity and the machine is a function of that identity alone, whatever else two requests differ in — and a request naming an identity the center holds no record of queues nothing at all. For a turn the fetch is not a step beside the start, it is what the start is conditional on: a refused fetch settles `failed` with the reason and hands the runner nothing, so nothing starts unarmed, what starts carries the credential the center holds under the same key that named it, and no other identity's credential is consulted in its place — a credentialless identity starts nothing rather than being served a neighbour's. A refusal is one intent's and not the beat's, so a half-armed fleet still runs the work it can. The same rule one layer down is the dialect: told a set of servers it cannot pass on, it must refuse rather than start with them dropped, or the advertised set and the enforced set are two sets (F8) — and of the three answers delivery has, none means "asked for, and delivered less". Beside all of that sits one label that decides none of it: the task the work is filed under is the one thing a caller names that the center does not derive, and it is optional — two requests differing in it still queue for one machine under one dialect, and the queue files work under exactly the label it was asked under, inventing no task for work that named none |
| `Formal/Authorize.lean` | `mcp-gateway/src/authorize.ts`, `mcp-surface.ts` | the listing is not a second rule that agrees with the call, it is the call asked once per tool, so a tool the surface shows is a tool `tools/call` allows and a tool it hides is one the call refuses; ask a shallower question instead — key each entry by its server rather than by its own tool — and the surface advertises a tool the call refuses. With no `/` in a name, `mcp://<serverId>/<tool>` determines the pair, so a grant for one tool is not a grant for another — `resource.ts` calls that a convention and does not enforce it, and a name that carries the separator gives two tools one verdict. A request that resolved to no principal lists nothing and refuses in full, and neither edge asks the engine |
| `Formal/Authz.lean` | `effect-authz/src/decide.ts` | deny wins wherever it sits, so entry order cannot change a verdict; visibility is the decision, not a second opinion |
| `Formal/Canonical.lean` | `canonical-json/src/index.ts` | a canonical encoding may reorder and may not drop: sorting keys is what the mechanism is for, but a value carrying its own `toJSON` — a `Date` above all — has no own keys to sort, so read as a record it encoded as `{}`, the encoding an empty object gets and the encoding every other date gets. Any two frames differing only in a timestamp are therefore one observation, and the change-detection hash that reads "unchanged" is a change the observer never records, while a schema comparison that reads "changed" refuses an upgrade the author only reordered; neither failure is loud. The rule was written out three times — in `effect-observe`, in `effect-compat` and in `agentd` — and the copies had begun to disagree. Visiting the value through `toJSON` first — what `JSON.stringify` does with it — tells two dates apart and leaves every other case where it was, and the step reaches a date nested in a record because a record is encoded by encoding its fields |
| `Formal/CardVerdict.lean` | `mantis/src/hosts/dingtalk/card/callback.ts` | the verdict is read from a field named `action`, so the tool's own arguments — which the card carries out to the operator — cannot decide their own approval; read from any token anywhere instead, a call whose argument spells one resolves itself, and a payload holding both reads as the permissive one. Two actions naming different verdicts are no verdict, so an injected token beside the real click is refused rather than obeyed, while a click echoed twice is still one answer |
| `Formal/CatalogLoad.lean` | `mcp-gateway/src/catalog-load.ts` | a listing that threw leaves the surface as it was, so an unreachable upstream keeps advertising what it last advertised instead of flapping; emptying it instead loses a live server's tools while the report reads the same. Every server looked at is in exactly one of the two lists, and neither list names a server that was not looked at; swallow the failure and a stale surface reads as a fresh one |
| `Formal/Chain.lean` | `effect-ui/src/screen.ts` | the navigation chain names only screens that exist and terminates |
| `Formal/CommandRun.lean` | `effect-server/src/client/console-command-console.ts` (`runsFromHere`), `console-action-registry.ts` | §6.4's Actions group is the one group whose `Enter` does something rather than going somewhere, so it is the one group where a wrong answer is a *write* — and the declarations carry no flag saying which actions destroy something, so the only signal there is is the method. The rule admits `GET` and nothing else, which makes the two readings complements: a call that goes somewhere runs from one keystroke exactly when it is a read, and every other method a declaration can name — `POST`, `PATCH`, `DELETE`, and anything the grammar has not been taught — is on the other side of that line. The reading that keeps only the argument rule is not a straw man: "it takes no arguments, so it is safe to repeat" is the natural simplification, and deleting one task by id is argument-free, so that reading is one `Enter`, one deleted record, and nothing asked. What the rule costs is refused actions sent to their own screen, which is the direction to be wrong in. The other half is *which* app's action a press may run: one app is mounted at a time, the app id is part of the registration, and it is checked again on the way out — so a press arriving in the moment between one app unmounting and the next mounting runs nothing at all rather than the wrong app's action, no name the view did not register runs, and the clear is guarded by the same id, so an app that unmounts after its replacement has registered leaves the replacement's slot exactly as it was |
| `Formal/CommandSearch.lean` | `effect-server/src/client/console-command-search.ts` | both rules here fail silently. The query narrows only if the terms *compose* as "every one of them": read as "any term", or as a score with a cutoff, typing widens the list instead, and nothing reports it — the palette simply offers more the more the operator types, which reads as a search that has not finished. And the composition is over a list, so an empty one holds of every row, which is what makes an all-whitespace query the full list rather than a blank pane. The row is matched against a fold of its label, caption, address and key, and occurrence composes — so a term found in a part is found in the fold, which is what makes an operation findable by its address and a command by the key it is bound to; drop a part and the row is quietly unfindable by it. The order is a stable partition and not a sort: the scoped half and the rest are each a filter of the rows, so §6.4's group order survives inside both halves, and the partition neither drops a row nor offers one twice |
| `Formal/Compat.lean` | `effect-compat/src/assess.ts` | a level that did not change is never adjudicated; the violations are exactly the changed levels |
| `Formal/Config.lean` | `effect-config/src/merge.ts` | the provenance names the layer the value came from; a new default cannot move an operator's override |
| `Formal/ConfigFetch.lean` | `agentd/src/control-config-ops.ts` | the machine's report moves the diff and never the config; the request cannot name the door; an agent holding no credential is refused, not handed an empty header |
| `Formal/Consent.lean` | `agentdeck/src/consent.ts` | the first answer to an ask is the answer; a write runs exactly when the operator approved it |
| `Formal/Credential.lean` | `agentd/src/adapter.ts`, `agentd/src/control-agent-ops.ts` | what a config presents decides the name its holder is reached by, and what the config says about itself decides nothing: the same request carrying one credential is one caller however it describes itself, while a door that read the claim would name one credential two ways, so anyone could carry another agent's sets by typing its id. The identity is spelled the door's way for the same reason — the door binds by the key a credential resolves to, so an agent named any other way is a set of bindings nothing reaches, and the center refuses the id rather than storing one. The credential itself is *declared*, not read per plan, and that is the property that chose the design: the gateway mints a fresh plaintext per issue and stores only its hash, so a center that asked each time would report a credential change on every plan, never settle the apply, and leave one more live credential for one identity per reading. Holding it is not a second copy of the gateway's record — that record is a hash and a revocation, and a plaintext is plaintext wherever it is used, which for an agent is a config file. The plan offered its own receipt reports nothing, an unverifiable credential carries nothing whatever the request asserted, a verified one for a principal no set is bound to carries nothing either, and the diff names the fields that moved and no others |
| `Formal/Derive.lean` | `effect-ui/src/screen-derive.ts` | a derived screen id collides with no other, declared or derived |
| `Formal/Door.lean` | `mantis/src/effect-ui-chat.ts`, `mantis/src/effect-ui-conversations.ts`, `effect-server/src/client/effect-ui-action-runtime.ts` | a control's gate and the press beside it name one path, so being offered and being addressable are one question — and that is a rule about two *names*, not two mechanisms, which is why breaking it is silent in both directions: name them apart and one store offers a control whose press makes no call at all while, read the other way, a call can be addressed that no control offers. The other half is a read offered through two doors — a row's `Read` and a `Start` field — and what makes them one read is that the url and the answer's path belong to the action while the value belongs to the press: so two doors of one action differ in exactly what they carried, not in where they answer, and the surface showing the answer shows the answer of whichever door was pressed. A second door cannot put a second answer somewhere the first is not read |
| `Formal/DoorAuth.lean` | `mcp-gateway/src/door-auth.ts` | the door answers with a credential and nothing else — it takes the two stores rather than the whole resolving surface, so the fields that let a *transport* vouch for what it passed through cannot be set by an HTTP request, and two requests presenting the same token are named alike whatever else they carry. Read the headers for an identity as well and anyone who can reach the port is whoever they say. A principal the door named was verified in four parts — a credential was presented, the store knows it, the grammar reads its key, and the directory is on for it — and every part is needed: a token the store never saw names nobody, a key the grammar cannot read names nobody, an identity switched off names nobody, while the converse holds, so the refusals are not vacuous. What the handlers are handed is the hash the record is listed by and the principal, never the secret that was presented |
| `Formal/Egress.lean` | `effect-network/src/policy.ts` | the exit a write leaves by is the one the policy chose and it was reachable; an unmet preference fails instead of borrowing another policy's exit |
| `Formal/Entry.lean` | `effect-ui/src/screen-spec.ts` (`UiScreen.onEnter`), `effect-server/src/client/effect-ui-action-runtime.ts` | a screen is complete on arrival by either door, and it is reached by two: a press, and an address pasted into the bar. Only the press used to read the record, so the same address showed a record when a press led to it and a heading over nothing when it was pasted — the parameters were in the address and nobody read them. What makes the two doors one is that the press writes what it holds into the address and the screen reads it back, so the claim is an agreement between a write and a read, and the write is a real step because the address is a string and not the object the press held: a key reads the value written if that was the key and otherwise exactly what it read before (which is `URLSearchParams` with the latest write in front, and why a later write shadows an earlier one without anything being removed); a value the press supplied, for a name the action declares, is what the screen reads on arrival and is the value the call was made with; the address carries the declared names and no others, so a screen cannot read a value no press wrote — not from an earlier arrival and not from another screen's parameters; and the call is skipped when *some* path parameter has no value, because read as "all of them missing", a press naming no task would send `/tasks/` and be answered about a path the declaration never described |
| `Formal/GateKey.lean` | `gate/src/gate/contract.ts` | the key a verdict is remembered under decides which later calls the operator is never asked about, because a recorded verdict is returned *before* `askWhen` runs — so the key is a projection of the request, and every field it drops is a way for one call to be answered by another's approval: dropping `session` answers another conversation from the first verdict, and dropping `access` answers a write from an approved read. Neither fails loudly; both return Allow, with no Ask and no card to the operator. Keying on the whole request leaves each to be asked about in its own right, and still remembers the request it was given for |
| `Formal/Generation.lean` | `effect-server/src/boot/reload.ts` | every refusal path leaves the serving generation serving; forgetting the restore leaves the app down. Nothing an attempt can decide before it registers — the app is not loaded, its manifest names no module, the module cannot be imported, or it exports another app — can be seen on the host at all |
| `Formal/InFlight.lean` | `effect-host/src/dispatch-counts.ts`, `dispatch-point.ts` | a release lands on the target its acquire named, so a target answering a request cannot read as drained; releasing whatever is current lets it read as drained and leaves the captured one never draining; the waiter is woken by the last release and stays quiet while anything is inside |
| `Formal/Launch.lean` | `agentd/src/launch-claim.ts` | the launch queue is a pull queue — a machine behind NAT cannot be called, so an intent waits until the machine it names asks for it — and claiming is what makes an intent that machine's own. The file wrote the reading a claim was made at and read it nowhere, so a claim had no end: a machine that claimed and then went away — the probe crashed, the host rebooted — left the intent `claimed` for good, no poll hands out what is not waiting, and the work that was asked for is never handed to anyone again at any time. Silence is the whole of the failure; the record reads as a machine's own forever. Give the claim a deadline and it is a lease rather than a transfer, and what it covered comes back when it lapses. Both bounds matter: the deadline must not take an intent from a machine that is still starting up, and it stops at `claimed` — once a machine has said it started the work may be half done, and handing it out again runs an agent twice in one directory. A `running` intent left by a machine that died mid-run stays visibly stuck, which is a record an operator can act on and not work quietly done twice |
| `Formal/Lease.lean` | `mcp-registry/src/store.ts`, `contract.ts` | the lease decides how a record ages, and `upsert` writes the lease of whichever caller ran last, so the two callers re-make each other's records: a registration naming a server that announced makes it static, and a static status answers `healthy` before it reads the clock — so the server reads healthy at any age from then on, over the `lastSeen` that said otherwise, and a heartbeat cannot move it. That is what `choose` acts on, which refuses a record that reads offline, so the registration puts back a server that had aged out. One call also means two dates: `upsert`'s fresh branch dates an unknown id `now` whatever the caller asked for, while its existing branch moves `lastSeen` only for the caller that renews |
| `Formal/Lifecycle.lean` | `effect-interface/src/registry.ts` | register and dispose are symmetric, and a stale disposer cannot revoke its replacement |
| `Formal/Match.lean` | `effect-authz/src/match.ts` | a `*` pins the depth, so a grant containing one reaches no further than it was written whatever it targets; a terminal `**` reaches strictly below its head and nothing shallower; a literal-only grant reaches below itself, and a named scheme reaches only its own. Dropping the depth pin lets `ops::*` reach every depth, and reading `**` as inclusive lets a container grant cover itself |
| `Formal/NodeGuard.lean` | `agentd/src/control-guard.ts` | `tokenRequired` and `authorized` are two readings of one fact, so a caller that skips the check and one that runs it cannot be told different things; an armed guard refuses a request that presents no token; writing the arm condition twice instead lets the two readings disagree |
| `Formal/ParentRoute.lean` | `effect-server/src/client/console-parent.ts` | §6.3's composed `Escape` ends "with no layer open, return to the parent screen", and this is the shell's half of that answer — the four places that have a second level. The claim is that the parent is the address with one part taken off, and its silent failure is a *cycle*: a parent that leads back makes `Escape` a key that appears to do nothing while it is in fact moving, which no operator can tell from a key that is genuinely inert. Every parent is strictly shallower than the route it came from, so no route is its own parent and no chain of presses can return to where it started; a second press has nothing to do, which is what makes the key land on the place and stop rather than walking a chain nobody declared; and a route with nowhere above it is exactly a route at the root — the same fact read from either side, so what the key can leave and what the address says about its own depth cannot disagree. Home and Not found answer `none` rather than guessing at a destination |
| `Formal/PluginRoute.lean` | `effect-host/src/routes.ts`, `dispatch.ts` | a plugin claims a path either by declaring `routes` or by its loaded plane supplying `canHandle`, and the file reads the first *in place of* the second — no union, no fallback — so a plugin that does both is answered by its routes alone, and one whose routes are an empty list is answered by nothing at all: an empty list reads as "no restriction" and means "no request", and the plane goes silent into the same 404 an unclaimed path gets. Read as a union, the plane the file silences comes back. The guards come first, so a plane that is off or not built is not consulted whatever it declared. The first plane in priority order that matches answers and no later one is asked, so a plane claiming a path another also claims takes the request for both |
| `Formal/PolicyScope.lean` | `script/src/policy.ts` | a derived child admits no tool its parent did not — but a scope's name list is what is *visible* under `allowlist` and what is *excluded* under `denylist`, so intersection is narrowing in one mode and the reverse in the other: intersect a denylist parent's exclusions with the child's and the child's list comes out empty, admitting every tool the parent was denied while the derivation reports success and the policy reads as valid. Excluding more where the names are exclusions is the same rule read in the mode it is in; excluding everything would satisfy the rule and derive an agent that can do nothing, so what neither side excluded stays admitted |
| `Formal/Presence.lean` | `agentd/src/presence-table.ts` | a lease's age is the larger of the two clock readings, so no wall-clock step backwards revives one the monotonic clock has run out on; reading the wall clock alone lets a step back revive a node silent for the length of the step. A step forward only expires leases early, so the maximum is not a trade. A lapsed presence starts over, so "up since" is not the last heartbeat and does not count the downtime as uptime. A nudge renews a presence that exists and has not been withdrawn — read as a plain renewal it puts a withdrawn node back online with no announce |
| `Formal/Preview.lean` | `mcp-gateway-app/src/access-preview.ts` | the console's access preview answers where the gateway answers — the gateway stops at the first bound set that reaches a server, so a preview that reads across every bound set lets a grant in a later set answer for a call the earlier one refuses, and the page says Allowed for a call that then fails elsewhere. The file also cleared its own reason list whenever it read Allowed, which is the line that would have shown the deny it was overriding. Where the first reachable set grants the two readings agree, which is why the bug needs a reachable first set; no reachable set at all is a denial in both. Agreeing by walking again is agreement by coincidence, so the preview no longer walks: it asks the gateway's own set registry and reads its answer, which makes the page's Allowed and the call's outcome two readings of one value rather than two computations that happen to match. The reason under the badge is the route the engine took, never a set the page chose, so a refusal always names one and an allowance never does; and the engine's `refusedBy` is absent exactly when the route admits, which is what lets the page treat "no reason" and "admitted" as one fact. The verdict is the engine's answer to a question the door can be *asked*: a name `tools/list` does not answer with stands for no pair, so the page refuses it — read the walk the engine can still make without a tool instead, which is what the file did, and the page says Allowed for a name no call could carry, for every agent bound to a set whose server is up |
| `Formal/Principals.lean` | `mcp-gateway/src/principals.ts` | a key the directory has never heard of is not active, so the same status read as "not disabled" admits any name at all; a registration writes `active` unconditionally and carries over only `createdAt`, so a disable is not sticky across a re-registration — the opposite of what "cut someone off with one flag here" predicts. A disable for an unknown key invents no record and reports `false`, so a typo'd key is refused rather than created; a `:` inside a kind or an id folds two principals onto one key |
| `Formal/Query.lean` | `state/src/store/contract.ts`, `memory.ts`, `storage-typeorm/src/store.ts` | a caller that asks a store for rows of a type gets a page, and the store chose the page — a hundred rows taken from the front, and nothing said so. So `checkpoint.list()`, read by its own protocol as "the checkpoints", answered with the first hundred ever written; `memory.entries` the same; and the gateway's audit, whose page is titled "the fifty most recent events", showed events fifty-one to a hundred of all time — a window that stops moving, since the hundred-and-first event the gateway carried was not in it nor any event after that, while the figure printed beside it as "what it has carried" went on being counted over the same frozen hundred. A bound the caller cannot see is a wrong answer, so the bound becomes the caller's: no limit is every row, and a limit is the newest that many, which makes the page the end of what an unbounded query would have returned. `wasPage` is the old rule, and the control shows the two answering differently on three rows |
| `Formal/Queue.lean` | `effect-host/src/queue.ts` | an operation finishing only drops the queue when it is still the tail, so the one queued behind it stays what the next submission waits for; without that check the next submission waits for nothing and runs beside it |
| `Formal/Readout.lean` | `effect-ui/src/readout.ts`, `source-status.ts` | a readout shows exactly when its path carries a value; a failed list never reads as an empty one |
| `Formal/Reasons.lean` | `mcp-gateway-app/src/access-reasons.ts` | a refusal is one of four facts about four different objects, and the page prints the first that holds — most specific first, which is the whole claim. A name no server offers comes before anything below it, because none of them can be about a tool that does not exist. An agent bound to nothing comes before the route, and it has to: a binding is the only way to a set, so "no bound set has a reachable server" is true whenever the agent is unbound — one denial, one cause further down — and a page reading the route first sends an operator to the registry for a server that was never what refused the call, which makes a typo'd or unissued identity look like a broken upstream. A set's two lists are written in the tool's own name, so the sentence quotes that name and not the advertised one: quote the advertised name and it sends the operator to add an entry the list is never compared against, which is an edit that changes nothing. The four are exhaustive, so Denied and "why" cannot come apart |
| `Formal/Redact.lean` | `mcp-gateway/src/redaction.ts`, `ai-gateway/src/redaction.ts` | the scrub leaves no field unvisited at any depth, for any vocabulary; a credential-named header is not carried |
| `Formal/Refresh.lean` | `packages/effect-ui/src/data-spec.ts` (`clear`, `refresh`), `effect-server/src/client/effect-ui-action-runtime.ts` | what a press leaves behind: an edit that does not name a path does not change what that path reads; a re-run read contributes a write at its own path and empties nothing; and the answer a press wrote is still where it wrote it after the reads it re-ran — which is why `clear` and `refresh` are two lists and not one, and the condition on the app is that a refresh must not name the path the press's own answer went to |
| `Formal/Reload.lean` | `effect-server/src/manifest-loader/generation.ts` | a generation number is handed out once, so a spent path is never served again; a commit keeps the serving copy and one rollback target, and discarding the displaced copy loses the rollback |
| `Formal/Replay.lean` | `effect-parity/src/replay.ts` | the monitor plane records the parity view it serves live, so a frame the agent perspective holds is a view — but a frame is typed by nothing except what its sampler returned, and only that perspective samples one. The file declared its own frame type, `id`/`capturedAt`/a `data` that is a view, and said it was the structural shape of the store's: a frame is filed under `at`, `perspective` and `target`, and its `data` is whatever was sampled. Read as the declaration said, every frame holds a view, so a frame recording anything else was read anyway and the fields it has no name for came back empty — an app with no state, rendering as nothing and saying nothing about why. Reading runs the check first, so a reading that is not a view reads as no view and the caller is told; the view that does come back is the frame's own, field for field. The control: the empty view is what the old reader returned for a frame that held a state, for every frame |
| `Formal/Resolve.lean` | `mcp-gateway/src/resolve.ts` | the three sources of a principal are read in strict order and a token that does not verify ends the search, so it cannot be downgraded to a claim bag or a forged header — sent back into the weaker sources, a bad token admits its bearer as whoever it named. With a good token present nothing else is read, so reading the claims first lets a valid token be rewritten. Bare headers are read only from a trusted transport; read always, anyone who can reach the port is an operator. Whoever is admitted, the registry has them |
| `Formal/Rollup.lean` | `board/src/tasks/rollup.ts` | a parent reads `done` only over leaves that finished or were abandoned, so a leaf still going keeps it from reading `done`; read as "some leaf is done" the parent finishes over a running child. A blocked leaf reaches its parent whatever sits beside it; check progress before the block and it disappears behind a running sibling. A node with children reports what is below it, not what it stored, which is why two nodes over the same leaves cannot disagree; keeping a parent's own copy lets one contradict its children |
| `Formal/Rules.lean` | `mcp-gateway/src/rules.ts` | the first rule that matches decides, not the first that denies, so an allow rule above a deny rule is a hole in it — the same two rules reordered flip the answer, the opposite of `effect-authz`'s deny-overrides one import away. A field left out of a rule's `match` bag is unconstrained rather than required absent, so a rule naming one field reaches every context agreeing on it, and a rule with no bag reaches every context there is. The fallback for a context no rule matches is `allow` in both places the file writes it, so an empty rule list is a policy of letting everything run |
| `Formal/Screen.lean` | `effect-ui/src/screen.ts` | entered screens form a chain back to the root, without repeats |
| `Formal/ServerListings.lean` | `mcp-gateway-app/src/server-listings.ts` | the Topology screen's answer to "why does the door offer nothing" is a fact about a server, so it goes in that server's own row — and it is three states, not two. `catalog-load.ts` reports what it loaded and what threw, and it is handed only the live servers, so one that is down is in neither list: a column reading the report alone paints a server that is down exactly as it paints one whose listing threw with a reason, and the page says nothing about the case an operator meets most often. Drawn from the round's own answer rather than computed beside it, the column cannot say `listed` for a server the door offers nothing through, nor name a failure the round never recorded |
| `Formal/SetSource.lean` | `mcp-gateway/src/set-source.ts`, `mcp-gateway/src/live-sets.ts`, `mcp-gateway/src/set-schema.ts` | a set and a binding are declared in the center that configures machines and enforced at the door, and between two apps in one host the thing that carries them is a *reader* rather than a copy — so there is no instant at which the two could disagree, which is exactly what the copy produced: a binding written in the center and a console on the other side reading a config nobody had written to. The slot takes one author, because a fact with two is the defect being removed, and a reload is not a second author but the same app saying the same thing again. The door rebuilds on the center's own write counter rather than on a clock, and that rests on one contract: a write never rewinds the counter and moves it whenever it changes a declaration, so a trace that did not move the counter did not move a set or a binding either — a door that only looks on a tick serves the binding that was replaced, for as long as the period, and the counter door serves it at once. The rebuild is total because the registry's guards are the grammar's questions asked again: the center refuses a set naming one server twice, a binding naming one set twice, and a set that both allows and denies one tool, in both directions, so a declaration that got past the grammar is one `registerSet`/`bindAgent` cannot throw on and the door cannot be left holding half a topology |
| `Formal/Sets.lean` | `mcp-gateway/src/sets.ts` | a set is reached only through a binding the agent holds, so naming a set it is not bound to reaches nothing; without that gate any agent names any set and gets its servers. A deny beats the same set's own allow list, so a tool written down as denied cannot be talked back in. The first bound set with a live server answers, so a set whose servers are all down yields the turn to the next — and a deny in that first set hides a grant in a later one, which is the file's order read literally |
| `Formal/Stack.lean` | `effect-server/src/client/console-stack.ts` | the session keeps one fact the address bar cannot answer — did this session walk here, or did the reader paste the address — because Back has to be two different things, and each has a wrong answer: `history.back()` from a pasted link leaves the product, and going to the parent screen from a walked link re-enters the screen the reader just left when they then press the browser's own back. The two readings the host takes off it are one question, so the control it draws and the key the browser handles cannot disagree: `canGoBack` is true exactly when the browser's back button lands somewhere. What the back button lands on after a push is the destination that was on top before it, so up one level is the level the reader came from; a back button pressed once pops exactly one entry and everything under it stays behind them; the push the module made itself is not an observation, so an address it set is not mistaken for a move the reader made; and a typed or pasted address is a jump the stack knows nothing about, which becomes the whole stack and is what leaves Back meaning "up to the screen's parent" |
| `Formal/Surface.lean` | `effect-apps/src/registration/surface.ts` | the pairing loop and the removal sweep partition the old surface, so a tool that disappears is reported once and an added tool not at all; without the sweep the dropped tool is not looked at and the upgrade passes. Both sides are read back from the registry, so a tool the new registry refused cannot read as present |
| `Formal/Swap.lean` | `effect-bundle/src/supervisor-swap.ts` | at every moment of a kernel swap the dispatcher points at a kernel that is loaded; stopping the old one first does not |
| `Formal/Timeline.lean` | `mantis/src/hosts/webui/console/ledger.ts`, `snapshot.ts` | the console keeps a conversation's timeline in memory and the host persists every turn, so a conversation that began before this process did has both — and the panel read them as alternatives, the live timeline if it had anything on it and the stored history otherwise. The live timeline starts empty, so the process's first message made it non-empty and the fifty turns already on disk stopped being rendered: the panel showed one entry where the conversation had fifty-one, and the conversation list beside it reported one turn, with nothing saying either thing. A timeline is opened onto the conversation instead of started beside it, and the numbers follow — the entry a turn lands on is its place in the conversation. The control: a timeline nobody opened onto numbers its next turn one, so a conversation's fifty-first turn is reported as its first |
| `Formal/Token.lean` | `mcp-gateway/src/token.ts` | whatever comes back was neither revoked nor expired, and the clock refuses at the deadline itself rather than a tick later; a hash the store has never seen verifies nowhere. Revocation is read before the clock, so a revoked token never verifies again whatever the reading. Verifying writes `lastUsedAt` and nothing else, so it cannot renew the lifetime — stamp a fresh one on as the record is used instead and a token used once per lifetime never reaches the end of one |
| `Formal/ToolKey.lean` | `effect-interface/src/tool-key.ts`, `effect-mcp/src/node-server/tools.ts` | a dot-free id splits a flattened tool key into exactly the pair that made it; two tool names can sanitize to one served name, and the surface refuses that instead of dropping a tool |
| `Formal/TreeOrder.lean` | `board/src/tasks/tree-order.ts` | no task is hidden: two tasks pointing at each other are below no root at any depth, so the walk down from the roots never hands them over, and the file's second walk hands over every task — everything handed over is listed. Nothing is listed twice, because an id already listed is not appended again; read without that guard the walk appends everything it is handed, and since the second walk hands over every task each one is listed twice |
| `Formal/UpstreamRewire.lean` | `mcp-gateway/src/registry-upstream.ts` | the reuse decision is worth exactly what the signature covers, and the signature covers every field an upstream is wired from — `connect` reads the endpoint and headers of an HTTP server and the command line and environment of a stdio one. Leave one out and a server that changed is handed back the upstream it had and the call still runs it, while the file's own signature would have told the two apart; that is the difference between the two. Every row's key is the signature of the server that row was wired from, which is what makes a matching key mean the same wiring. A rebuild that does not close what it displaces ends at the same table, so only the client still running says so; the 404 path without its remove leaves a client behind for a server the resolver stopped answering for. `serverId` is outside the signature and the upstream does index by it — answered 404 rather than the wrong server, so a test and not this |
| `Formal/Visibility.lean` | `script/src/closure.ts` | the file's claim is an invariant — everything visible has its dependencies visible — and neither mode's set satisfies it: a seed can name a tool whose dependency the registry does not have, and an exclusion can cut a chain in half, so the tool stays visible while the thing it calls does not and the agent finds out at the call, with the tool set having read as valid throughout. Closing the set is what both modes share, so they converge on the invariant rather than on the set; the tool whose dependency is absent is not visible under either, which is the cost of the rule and the reason it holds |
| `Formal/Watch.lean` | `effect-server/src/boot/watch.ts` | an event is judged by the file's own time against the last reaction, so the reports a reaction itself causes — a path it only moved — are not edits and do not start the next reload; judging by the event instead makes every copy another reload. A burst for one app leaves one timer armed, carrying the last event, and touches no other app's timer |

## What this is not

The model is of the *rule*, not of the code that applies it: each module states
the invariant a file owes and proves it about a small idealisation of that file,
with the idealisation noted where it matters. A proof here means the rule holds,
and it means a change to the rule that breaks it fails `check:proofs` — not that
the TypeScript is verified.

Most of the repository is not modelled. The rule of thumb for what to add: a
mechanism whose correctness is a claim in a comment, where the wrong answer is
silent — a provenance that lies, a chain that loops, a disposer that revokes the
wrong registration. A mechanism that fails loudly when it is wrong needs a test,
not a proof.
